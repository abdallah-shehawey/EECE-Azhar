# Local admin tooling — contains student name→email mapping, never publish.
scripts/set-student-emails.mjs

# Firebase data backups (contain student emails) — local safety copies only.
scripts/backup/

# Secrets / env (never commit)
.env
.env.*
*.secret

# OS / editor cruft
.DS_Store
node_modules/

# Throwaway local preview/screenshot scaffolds (generated from index.html)
_*.html

# Local admin tooling — student name→email mapping, never publish.
scripts/assign-emails.js
